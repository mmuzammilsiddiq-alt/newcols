[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install", "cd server && npm install"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node server/index.js"
